<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WindowNoTitleActivityTheme" parent="AppTheme">
        <item name="windowNoTitle">true</item>
    </style>

    <style name="WindowFullScreenActivityTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="WindowContentOverlayTheme" parent="AppTheme">
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="WindowBackgroundTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/alivc_lanch_loading</item>
    </style>

</resources>
